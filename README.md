# fairbayrank_sauv
Adversarial learning in recsys
perform firstly the data generation and preprocessing by using several generator files
extract samples
extract triplets of positive, negative user feedback and sensitive attribute
the dataset made of triplets is segmented in training, validation and test. Set up respectively percentage
after the data generation and preprocessing, set up hyperparameters to train the model

